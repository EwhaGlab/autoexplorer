local_costmap:

    global_frame: sensor # was odom
    robot_base_frame: sensor
    publish_frequency: 6.0 # was 3
    
    static_map: false # must be false
    rolling_window: true
    
    width: 24.0
    height: 24.0
    resolution: 0.05 #0.25

    plugins:
    - {name: obstacles, type: "costmap_2d::ObstacleLayer"}
    - {name: inflater,  type: "costmap_2d::InflationLayer"}

    #obstacles:
        #publish_observations: false

    #obstacles:
        #observation_sources: base_scan
        #base_scan:
            #data_type: PointCloud2 #LaserScan
            #topic: sensor_scan #base_scan
            #marking: true
            #clearing: true
            #obstacle_range: 8.0   # 4.0
            #raytrace_range: 10.0  # 5.0
            #min_obstacle_height: 0.25
            #max_obstacle_height: 0.3
        #z_voxels: 16
        #z_resolution: 0.125
        #max_obstacle_height: 2.0
        
